# WebComponent

The base class that all Proscenium::UI custom elements extend. Lives at
`lib/proscenium/ui/web_component.js`.

The pages in this section are wired up as system-test fixtures for
`test/system/web_component_test.rb` and are not intended as styled examples — they verify
the public surface of `WebComponent`.
